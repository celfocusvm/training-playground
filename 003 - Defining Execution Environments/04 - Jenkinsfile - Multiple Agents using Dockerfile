pipeline {
    agent none
    stages {
    	stage{
	    agent {
    		dockerfile {
            		dir '003 - Defining Execution Environments'
            		filename '04 - Dockerfile - Java'
        	}
    	    }
	    steps {
	    	sh "mvn −version"
	    }
	}
	stage{
	    agent {
    		dockerfile {
            		dir '003 - Defining Execution Environments'
            		filename '04 - Dockerfile - NPM'
        	}
    	    }
	    steps {
	    	sh "node -v"
	    }
	}
    }
}
